# Machine Doctor AI 서비스 기획서

## 1. 서비스 개요

### 서비스명

**Machine Doctor AI**

### 서비스 한 줄 소개

공작기계에서 발생하는 이상 현상과 발생 조건을 입력하면 AI가 예상 원인, 점검 항목 및 권장 조치를 제안하는 AI 기반 웹서비스입니다.

### 개발 목적

공작기계에서 품질 문제나 이상 현상이 발생했을 때 사용자가 문제 내용을 입력하면 AI가 입력 내용을 분석하여 초기 점검 방향을 제시하도록 하는 것을 목적으로 합니다.

사용자가 문제 원인을 검토할 때 참고할 수 있는 정보를 쉽고 빠르게 제공하는 웹서비스를 구현합니다.

---

## 2. 타깃 사용자

Machine Doctor AI의 주요 사용자는 다음과 같습니다.

- 공작기계 품질 엔지니어
- 공작기계 생산 엔지니어
- 공작기계 서비스 엔지니어
- 공작기계 유지보수 담당자
- 생산 현장의 설비 관리 담당자

사용자가 공작기계에 대한 이상 현상을 입력하면 AI가 점검 방향을 제안함으로써 초기 문제 분석을 지원합니다.

---

## 3. 서비스 핵심 기능

Machine Doctor AI의 핵심 기능은 **AI 기반 공작기계 이상 현상 분석**입니다.

사용자가 다음 정보를 입력합니다.

- 이상 현상
- 발생 조건

AI는 입력 내용을 분석하여 다음 정보를 제공합니다.

- 예상 원인
- 점검 항목
- 권장 조치

전체 기능 흐름은 다음과 같습니다.

```text
공작기계 이상 발생
        ↓
사용자가 이상 현상 입력
        ↓
발생 조건 입력
        ↓
AI 분석하기
        ↓
OpenAI API 분석
        ↓
예상 원인
점검 항목
권장 조치
        ↓
사용자가 결과 확인
```

---

## 4. 페이지 및 섹션 구성

Machine Doctor AI는 사용자가 쉽게 이용할 수 있도록 4개의 주요 섹션으로 구성합니다.

### 4.1 HOME

서비스의 첫 화면입니다.

주요 내용:

- Machine Doctor AI 서비스명
- 서비스 핵심 설명
- AI 분석 시작하기 버튼

사용자는 AI 분석 시작하기 버튼을 통해 AI 품질분석 영역으로 이동할 수 있습니다.

### 4.2 서비스 소개

Machine Doctor AI의 목적과 주요 기능을 설명합니다.

주요 내용:

- 서비스 개발 목적
- AI 분석 기능 소개
- 사용자에게 제공하는 정보

### 4.3 AI 품질분석

Machine Doctor AI의 핵심 기능입니다.

사용자가 공작기계의 이상 현상과 발생 조건을 입력하고 AI 분석을 실행합니다.

화면 구성:

```text
[이상 현상]

예:
주축 회전 시 고주파 소음과 진동이 발생합니다.


[발생 조건]

예:
8000rpm 이상에서 소음이 증가하고
주축 온도도 함께 상승합니다.


[ AI 분석하기 ]


[ AI 분석 결과 ]

예상 원인

점검 항목

권장 조치
```

### 4.4 사용방법

Machine Doctor AI 사용 순서를 설명합니다.

1. 공작기계의 이상 현상을 입력합니다.
2. 이상이 발생하는 조건을 입력합니다.
3. AI 분석하기 버튼을 클릭합니다.
4. AI가 생성한 분석 결과를 확인합니다.
5. 예상 원인과 점검 항목을 참고하여 실제 장비를 점검합니다.

---

## 5. AI 기능 설계

### 5.1 AI 입력

AI 분석에 사용되는 주요 입력 데이터는 다음과 같습니다.

**이상 현상**

예:

```text
주축 회전 시 고주파 소음과 진동이 발생합니다.
```

**발생 조건**

예:

```text
8000rpm 이상에서 소음이 증가하고
주축 온도도 함께 상승합니다.
```

---

## 6. AI 출력

OpenAI API를 통해 입력 내용을 분석하고 다음과 같은 형태의 정보를 제공합니다.

### 예상 원인

입력된 이상 현상과 발생 조건을 바탕으로 가능한 문제 원인을 제안합니다.

예:

```text
1. 주축 베어링 상태 이상 가능성
2. 회전체 밸런스 문제 가능성
3. 고속 회전 영역에서의 공진 가능성
```

### 점검 항목

문제 원인을 확인하기 위해 필요한 점검 항목을 제안합니다.

예:

```text
1. 주축 진동 측정
2. FFT 주파수 분석
3. 주축 온도 확인
4. 회전속도별 진동 변화 확인
```

### 권장 조치

점검 결과에 따라 사용자가 검토할 수 있는 조치 방향을 제안합니다.

AI 분석 결과는 참고용으로 제공하며 실제 장비의 최종 진단은 측정 결과 및 전문가의 판단이 필요합니다.

---

## 7. AI 기능 데이터 흐름

Machine Doctor AI의 Frontend와 Backend 데이터 흐름은 다음과 같습니다.

```text
사용자
   ↓
이상 현상 / 발생 조건 입력
   ↓
index.html
   ↓
JavaScript (app.js)
   ↓
fetch('/api/analyze')
   ↓
Python Backend (api/analyze.py)
   ↓
OpenAI API
   ↓
AI 분석 결과
   ↓
Python Backend
   ↓
JavaScript
   ↓
웹페이지 결과 표시
```

---

## 8. 실패 처리 기준

AI 기능 사용 과정에서 발생할 수 있는 문제에 대해 사용자에게 안내 메시지를 제공합니다.

### 8.1 필수 입력 누락

사용자가 이상 현상을 입력하지 않고 AI 분석하기 버튼을 클릭하는 경우:

```text
분석할 이상 현상을 입력해주세요.
```

메시지를 표시합니다.

### 8.2 AI 분석 중

AI API의 응답을 기다리는 동안:

```text
AI가 분석 중입니다. 잠시만 기다려주세요...
```

메시지를 표시하여 현재 처리 상태를 사용자에게 알려줍니다.

### 8.3 API 오류

OpenAI API 또는 Backend 통신 과정에서 오류가 발생하는 경우 사용자에게 오류 안내 메시지를 표시합니다.

사용자가 오류 발생 사실을 확인하고 다시 시도할 수 있도록 구성합니다.

---

## 9. 반응형 웹 설계

Machine Doctor AI는 다음 환경에서 사용할 수 있도록 반응형 웹으로 구현합니다.

- 데스크톱
- 태블릿
- 스마트폰

화면 크기가 작아지면 메뉴와 콘텐츠의 배치가 자동으로 조정되도록 CSS Media Query를 적용합니다.

이를 통해 모바일 환경에서도 입력창, AI 분석 버튼 및 분석 결과를 확인할 수 있도록 구성합니다.

---

## 10. 기술 구성

### Frontend

```text
HTML
CSS
JavaScript
```

### Backend

```text
Python
Vercel Serverless Functions
```

### AI

```text
OpenAI API
```

### 소스 관리

```text
Git
GitHub
```

### 배포

```text
Vercel
```

---

## 11. API Key 보안

OpenAI API Key는 소스코드에 직접 입력하지 않습니다.

로컬 개발 환경에서는 `.env` 파일을 사용합니다.

```text
OPENAI_API_KEY=본인의_API_Key
```

`.gitignore`에 `.env`를 등록하여 GitHub에 API Key가 업로드되지 않도록 관리합니다.

Vercel 배포 환경에서는 Vercel의 Environment Variables 기능을 사용하여 `OPENAI_API_KEY`를 등록합니다.

---

## 12. 기대 효과

Machine Doctor AI를 통해 다음과 같은 효과를 기대할 수 있습니다.

- 공작기계 이상 현상의 초기 분석 지원
- 문제 발생 시 점검 방향 제시
- 점검 항목 검토 지원
- AI를 활용한 기술 업무 지원 가능성 확인
- 웹 기반 서비스로 PC와 모바일에서 접근 가능

---

## 13. 서비스 이용 시 주의사항

Machine Doctor AI가 제공하는 결과는 AI가 입력 내용을 기반으로 생성한 **참고 정보**입니다.

AI 분석 결과만으로 실제 공작기계의 고장 원인을 확정하지 않습니다.

실제 품질 문제 및 설비 이상에 대한 최종 판단은 다음 내용을 종합하여 수행해야 합니다.

- 실제 장비 상태
- 측정 데이터
- 정비 이력
- 작업 환경
- 엔지니어의 전문적인 판단

---

## 14. 최종 서비스 목표

Machine Doctor AI의 최종 목표는 다음과 같습니다.

```text
사용자 문제 입력
        ↓
AI 분석
        ↓
예상 원인 제안
        ↓
점검 항목 제안
        ↓
권장 조치 제안
        ↓
실제 문제 분석 지원
```

AI 코딩 도구를 활용하여 아이디어 기획부터 Frontend 개발, Python Backend 구현, OpenAI API 연동, GitHub 소스 관리 및 Vercel 배포까지 하나의 실제 AI 웹서비스 개발 과정을 완성하는 것을 목표로 합니다.